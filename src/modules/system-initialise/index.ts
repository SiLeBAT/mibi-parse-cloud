/**
 * The system initialize module includes functions that:
 * - set the system up to function properly and check system is working as planned
 */
import { logger } from '../../system/logging';
import './use-cases';

logger.info('Parse Cloud: System initialize module loaded.');
