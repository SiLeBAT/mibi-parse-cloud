/**
 * The system monitoring module includes functions that:
 * - through an api give details about the health and state
 * of the running system
 */
import { logger } from '../../system/logging';
import './useCases';
logger.info('Parse Cloud: System monitoring module loaded.');
